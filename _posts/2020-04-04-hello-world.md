---
layout: post
title: Hello World!
description: My First Post
summary: My first post to give this blog a spin
tags: hello math
---

Hello there! This is my first post. 

This isn't really meant to have any true "meat" to it. It's just a way for me to make sure that everything I anticipate in including in my posts will be able to work and display properly. 

Here's a "fun" integral (just to test if MathJax is working properly)

$$ \int^{\infty}_{-\infty} \frac{dx}{(e^{x} - x)^2 + \pi^2} = \frac{1}{1+\Omega} $$

Along with a little bit of inline MathJax: $$x$$, $$y$$, $$z$$, $$\rho$$

Now lets go for syntax highlighting, first with Ruby:

```ruby
def foo
  puts 'foo'
end
```
Now we shoot for Python:

```python
for i in range(10):
    print(i)
```

And let's do C for safe call

```c
void XorSwap( int* x, int* y ) 
{
  if (x != y)
  {
    *x ^= *y;
    *y ^= *x;
    *x ^= *y;
  }
}
```

Time for some pictures as well!

![A sleeping cat](/images/sleeping_cat.jpg)

And that's a wrap! Looks like the website is working a-okay!

Time for me to hit the hay (2:14 AM as I write this).